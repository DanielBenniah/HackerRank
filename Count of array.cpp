#include<iostream>
using namespace std;
//#define swap(a,b) {int temp=a;a=b;b=temp;}
int main()
{
	int array[10];
	int count;
	for(count=0;count<10;count++)
	count[array]=count;
	if(count==10)
	cout<<"Successful";
	else
	cout<<"Unsuccessful";
	return 0;	
}

